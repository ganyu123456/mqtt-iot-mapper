apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mqtt-iot-mapper.configmapName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mqtt-iot-mapper.labels" . | nindent 4 }}
data:
  configData: |
    {{- .Values.mapperConfig | toYaml | nindent 4 }}
